version: '3'
services:
  python:
    image: python
    environment:
      MYSQL_DATABASE: mysql
      MYSQL_USER: roots
      MYSQL_PASSWORD: rootroot
      MYSQL_ROOT_PASSWORD: rootroot
    ports:
    - "3307:3306"
    volumes:
    - ./db_data:/var/lib/mysql